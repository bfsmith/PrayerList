<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="bs.howdy.PrayerList"
      android:versionCode="1" android:versionName="1.0.0">
    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="16"/>
	<application android:icon="@drawable/icon" android:label="@string/app_name" android:name="bs.howdy.PrayerList.App" android:enabled="true">
        <activity android:label="@string/app_name" 
        	android:name=".Activities.PrayerListMainActivity"
        	android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"></category>
                <action android:name="android.intent.action.MAIN"></action>
            </intent-filter>
        </activity>
		<activity android:name=".Activities.ActivePrayers" android:theme="@android:style/Theme.NoTitleBar" />
		<activity android:name=".Activities.AnsweredPrayers" android:theme="@android:style/Theme.NoTitleBar" />
		<activity android:name=".Activities.CreateEditPrayer" android:theme="@android:style/Theme.NoTitleBar" />
		<activity android:name=".Activities.PrayerInfo" android:theme="@android:style/Theme.NoTitleBar" />
    </application>
</manifest>